name: CI

# Every pull request and every push to a long-lived branch is checked. The
# `/test` comment remains as a manual re-run - it is the only trigger that can
# re-check an unchanged head, which is what a flake or a fixed base needs.
on:
  push:
    branches: [main, dev]
  pull_request:
  issue_comment:
    types: [created]

# Read-only by default. The jobs below check out and run the pull request's own
# code, so the token they inherit must not be able to write anything; only the
# job that posts the result comment gets more.
permissions:
  contents: read

# A push supersedes the run already in flight for the same ref. The comment
# path groups by issue number instead, so a manual re-run and the automatic run
# for the same pull request never cancel each other.
concurrency:
  group: ci-${{ github.event.issue.number || github.ref }}
  cancel-in-progress: true

jobs:
  gate:
    name: Check trigger
    # A push or pull request is its own authorization and runs unconditionally.
    # A comment has to be on a pull request and has to start with `/test`.
    if: >-
      github.event_name != 'issue_comment' ||
      (github.event.issue.pull_request && startsWith(github.event.comment.body, '/test'))
    runs-on: ubuntu-latest
    # Reacts to the triggering comment.
    permissions:
      contents: read
      pull-requests: write
    # Empty on the automatic path, which is what makes `actions/checkout` fall
    # back to the ref the event itself carries - the merge commit for a pull
    # request, the pushed commit for a push.
    outputs:
      sha: ${{ steps.pr.outputs.sha }}
    steps:
      - name: Verify commenter has write access
        id: perm
        if: github.event_name == 'issue_comment'
        uses: actions/github-script@v9
        with:
          script: |
            const { data } = await github.rest.repos.getCollaboratorPermissionLevel({
              owner: context.repo.owner,
              repo: context.repo.repo,
              username: context.payload.comment.user.login,
            });
            const allowed = ['write', 'maintain', 'admin'].includes(data.permission);
            if (!allowed) {
              core.setFailed(`@${context.payload.comment.user.login} does not have write access (has: ${data.permission})`);
            }
      - name: Resolve PR head SHA
        id: pr
        if: github.event_name == 'issue_comment'
        uses: actions/github-script@v9
        with:
          script: |
            const { data: pr } = await github.rest.pulls.get({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: context.issue.number,
            });
            core.setOutput('sha', pr.head.sha);
      - name: React to comment
        if: github.event_name == 'issue_comment'
        uses: actions/github-script@v9
        with:
          script: |
            await github.rest.reactions.createForIssueComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              comment_id: context.payload.comment.id,
              content: 'rocket',
            });

  frontend:
    name: Frontend
    needs: gate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ needs.gate.outputs.sha }}
          # This job runs the checked-out code; it must not keep a usable token.
          persist-credentials: false
      # Must be at least the bun that wrote bun.lock: an older one cannot read
      # the lockfile format and `--frozen-lockfile` then fails outright. Keep
      # this in step with `packageManager` in package.json.
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.0
      - run: bun install --frozen-lockfile
      - run: bun test
      - run: bun run lint
      - run: bun run build

  rust:
    name: Rust (${{ matrix.os }})
    needs: gate
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, macos-latest, ubuntu-latest]
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ needs.gate.outputs.sha }}
          # This job runs the checked-out code; it must not keep a usable token.
          persist-credentials: false
      - name: Install Linux build dependencies
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libgtk-3-dev librsvg2-dev patchelf
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: "./src-tauri -> target"
      - run: cargo clippy --manifest-path src-tauri/Cargo.toml --locked --all-targets -- -D warnings
      - run: cargo test --manifest-path src-tauri/Cargo.toml --locked

  report:
    name: Report result
    needs: [gate, frontend, rust]
    # Comment path only. A push or pull request already shows its result as a
    # status check, and a comment per run would be noise on every push.
    if: always() && github.event_name == 'issue_comment' && needs.gate.result == 'success'
    runs-on: ubuntu-latest
    # Posts the result comment. Checks out nothing, so it never runs PR code.
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/github-script@v9
        with:
          script: |
            const ok = '${{ needs.frontend.result }}' === 'success' && '${{ needs.rust.result }}' === 'success';
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: ok
                ? `✅ CI passed for ${{ needs.gate.outputs.sha }}`
                : `❌ CI failed for ${{ needs.gate.outputs.sha }} — see [run](${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId})`,
            });
